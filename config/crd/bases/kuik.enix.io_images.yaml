---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: images.kuik.enix.io
spec:
  group: kuik.enix.io
  names:
    kind: Image
    listKind: ImageList
    plural: images
    shortNames:
    - img
    singular: image
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.registry
      name: Registry
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .status.usedByPods.count
      name: Pods count
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Image is the Schema for the images API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ImageSpec defines the desired state of Image.
            properties:
              image:
                description: Image is a string identifying the image
                type: string
              registry:
                description: Registry is the registry where the image is located
                type: string
            required:
            - image
            - registry
            type: object
          status:
            description: ImageStatus defines the observed state of Image.
            properties:
              upstream:
                description: Upstream is the information about the upstream image
                properties:
                  digest:
                    description: Digest is the digest of the upstream image manifest,
                      if available
                    type: string
                  lastMonitor:
                    description: LastMonitor is the last time a monitoring task for
                      the upstream image was was started
                    format: date-time
                    type: string
                  lastSeen:
                    description: LastSeen is the last time the image was seen upstream
                    format: date-time
                    type: string
                  mediaType:
                    description: MediaType is the media type of the upstream image
                      manifest, if available
                    type: string
                type: object
              usedByPods:
                description: UsedByPods is the list of pods using this image
                properties:
                  count:
                    description: |-
                      Count is the number of objects using this image

                      jsonpath function .length() is not implemented, so the count field is required to display objects count in additionalPrinterColumns
                      see https://github.com/kubernetes-sigs/controller-tools/issues/447
                    type: integer
                  items:
                    description: Items is a list of reference to objects using this
                      Image
                    items:
                      type: string
                    type: array
                type: object
            type: object
        type: object
    selectableFields:
    - jsonPath: .spec.registry
    served: true
    storage: true
    subresources:
      status: {}
