---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: imagemonitors.kuik.enix.io
spec:
  group: kuik.enix.io
  names:
    kind: ImageMonitor
    listKind: ImageMonitorList
    plural: imagemonitors
    shortNames:
    - imgmon
    singular: imagemonitor
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.registry
      name: Registry
      type: string
    - jsonPath: .spec.interval
      name: Interval
      type: string
    - jsonPath: .spec.burst
      name: Burst
      type: integer
    - jsonPath: .status.lastExecution
      name: Last Execution
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ImageMonitor is the Schema for the imagemonitors API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ImageMonitorSpec defines the desired state of ImageMonitor.
            properties:
              burst:
                default: 1
                description: Burst is the number of images to check in parallel, defaults
                  to 1
                minimum: 1
                type: integer
              interval:
                description: Interval is the interval at which the image monitor checks
                  for updates
                type: string
              registry:
                description: Registry is the registry to monitor for image updates,
                  it filters local image to check upstream
                type: string
            required:
            - interval
            - registry
            type: object
          status:
            description: ImageMonitorStatus defines the observed state of ImageMonitor.
            properties:
              LastExecution:
                description: LastExecution is the last time the image monitor checked
                  for updates
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
