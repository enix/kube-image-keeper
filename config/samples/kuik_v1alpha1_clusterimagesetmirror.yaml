apiVersion: kuik.enix.io/v1alpha1
kind: ClusterImageSetMirror
metadata:
  labels:
    app.kubernetes.io/name: kube-image-keeper
    app.kubernetes.io/managed-by: kustomize
  name: clusterimagesetmirror-sample
spec:
  # TODO(user): Add fields here
