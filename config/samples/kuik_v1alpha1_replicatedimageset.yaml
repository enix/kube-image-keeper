apiVersion: kuik.enix.io/v1alpha1
kind: ReplicatedImageSet
metadata:
  labels:
    app.kubernetes.io/name: kube-image-keeper
    app.kubernetes.io/managed-by: kustomize
  name: replicatedimageset-sample
spec:
  # TODO(user): Add fields here
