apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: clusterimagesetmirrors.kuik.enix.io
spec:
  group: kuik.enix.io
  names:
    kind: ClusterImageSetMirror
    listKind: ClusterImageSetMirrorList
    plural: clusterimagesetmirrors
    shortNames:
    - cism
    singular: clusterimagesetmirror
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterImageSetMirror is the Schema for the clusterimagesetmirrors
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterImageSetMirrorSpec defines the desired state of ClusterImageSetMirror.
            properties:
              cleanup:
                description: Cleanup defines a cleanup strategy
                properties:
                  enabled:
                    type: boolean
                  retention:
                    type: string
                type: object
              filter:
                description: |-
                  Filter selects which pods, namespaces and images this resource applies
                  to. It replaces the deprecated imageFilter.
                properties:
                  exclude:
                    items:
                      description: |-
                        ClusterFilterItem is a FilterItem extended with the namespace dimension,
                        available only on cluster-scoped resources.
                      properties:
                        annotation:
                          description: Annotation is a Kubernetes label-selector string
                            matched against the Pod's annotations.
                          maxLength: 512
                          type: string
                        image:
                          description: Image is a regular expression matched against
                            the normalised image reference.
                          maxLength: 128
                          type: string
                        label:
                          description: |-
                            Label is a Kubernetes label-selector string matched against the Pod's labels
                            (k8s.io/apimachinery/pkg/labels syntax: "key=value", "key!=value", "key",
                            "!key", "key in (a,b)", "key notin (a,b)").
                          maxLength: 512
                          type: string
                        namespace:
                          description: Namespace is a regular expression matched against
                            the Pod's namespace.
                          maxLength: 128
                          type: string
                      type: object
                    maxItems: 16
                    type: array
                    x-kubernetes-validations:
                    - message: each filter item must set exactly one of image, label,
                        annotation or namespace
                      rule: 'self.all(item, (has(item.image) ? 1 : 0) + (has(item.label)
                        ? 1 : 0) + (has(item.annotation) ? 1 : 0) + (has(item.namespace)
                        ? 1 : 0) == 1)'
                  include:
                    items:
                      description: |-
                        ClusterFilterItem is a FilterItem extended with the namespace dimension,
                        available only on cluster-scoped resources.
                      properties:
                        annotation:
                          description: Annotation is a Kubernetes label-selector string
                            matched against the Pod's annotations.
                          maxLength: 512
                          type: string
                        image:
                          description: Image is a regular expression matched against
                            the normalised image reference.
                          maxLength: 128
                          type: string
                        label:
                          description: |-
                            Label is a Kubernetes label-selector string matched against the Pod's labels
                            (k8s.io/apimachinery/pkg/labels syntax: "key=value", "key!=value", "key",
                            "!key", "key in (a,b)", "key notin (a,b)").
                          maxLength: 512
                          type: string
                        namespace:
                          description: Namespace is a regular expression matched against
                            the Pod's namespace.
                          maxLength: 128
                          type: string
                      type: object
                    maxItems: 16
                    type: array
                    x-kubernetes-validations:
                    - message: each filter item must set exactly one of image, label,
                        annotation or namespace
                      rule: 'self.all(item, (has(item.image) ? 1 : 0) + (has(item.label)
                        ? 1 : 0) + (has(item.annotation) ? 1 : 0) + (has(item.namespace)
                        ? 1 : 0) == 1)'
                type: object
              imageFilter:
                description: ImageFilter selects which images to mirror. Superseded
                  by filter.
                properties:
                  exclude:
                    items:
                      maxLength: 128
                      type: string
                    maxItems: 16
                    type: array
                    x-kubernetes-validations:
                    - message: exclude contains an invalid regular expression
                      rule: 'self.all(p, ''''.matches(p) ? true : true)'
                  include:
                    items:
                      maxLength: 128
                      type: string
                    maxItems: 16
                    type: array
                    x-kubernetes-validations:
                    - message: include contains an invalid regular expression
                      rule: 'self.all(p, ''''.matches(p) ? true : true)'
                type: object
              mirrors:
                items:
                  properties:
                    cleanup:
                      description: Cleanup defines a cleanup strategy
                      properties:
                        enabled:
                          type: boolean
                        retention:
                          type: string
                      type: object
                    credentialSecret:
                      properties:
                        name:
                          description: Name is the name of the secret
                          type: string
                        namespace:
                          description: |-
                            Namespace is the namespace where the secret is located.
                            This value is ignored for namespaced resources and the namespace of the parent object is used instead.
                          type: string
                      type: object
                    path:
                      type: string
                    priority:
                      description: |-
                        Priority controls the ordering of this mirror in comparaison to similar alternatives (mirrors with same parent priority) when re-routing images.
                        0 means no specific ordering (YAML declaration order is preserved).
                        Positive values are sorted ascending: lower value = higher priority.
                      type: integer
                    registry:
                      type: string
                  type: object
                type: array
              priority:
                description: |-
                  Priority controls the ordering of alternatives from this CR relative to the original image and other CRs.
                  Negative values place alternatives before the original image; positive values place them after.
                  Default is 0 (original image first, then alternatives in default type order).
                type: integer
            type: object
          status:
            description: ClusterImageSetMirrorStatus defines the observed state of
              ClusterImageSetMirror.
            properties:
              matchingImages:
                items:
                  properties:
                    image:
                      type: string
                    mirrors:
                      items:
                        properties:
                          image:
                            type: string
                          lastError:
                            type: string
                          mirroredAt:
                            format: date-time
                            type: string
                        required:
                        - image
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - image
                      x-kubernetes-list-type: map
                    unusedSince:
                      format: date-time
                      type: string
                  required:
                  - image
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - image
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
