quelques changements à faire concernant la spec v3. quand quelque chose est changé, ne pas dire "avant c'était comme ça, maintenant c'est comme ça", directement dire comment c'est. éviter les négations et les comparaisons avec la v2. rester succint mais explicite.

## Corrections de doc

1. [x] sélection de la source explicitée dans A.8 du walkthrough ImageMirror (origine d'abord, sinon une alternative qui matche en ordre déclaré, `unavailable: true` écartée, fallback valable en première copie **et** en re-copie), destination toujours dérivée de l'origine, avec les 3 conséquences (credentials de la source retenue, digest du fast path multi-cluster, `source-ref`/`source-digest`) et le changement de queue. spec.md aligné : la restriction "and was never copied" est levée. **à faire valider par Aurélien** : rev4 autorise le fallback en re-copie, l'ancien spec.md le limitait à la première copie.
2. [x] digest-pinning réconcilié avec rev4 : une copie porte toujours le tag dérivé de l'origine quand la ref en a un (`repo:1.27_cluster-a@sha256:ab…`, résolu par digest, tag informatif pour la CRI mais attendu par le desired state), plus un **anchor tag** `sha256-<D>_<clusterID>` dès qu'il y a un digest — indispensable sous `driftPolicy: Sync`, qui repointe l'autre tag sur le nouveau digest upstream. vocabulaire unifié sur "anchor tag" (le "keeper tag" de spec.md a disparu), mapping ref -> tags donné par forme de référence dans A.6 et repris tel quel par le diff de cleanup en C.3.
3. [ ] spec.md ne doit pas décrire comment se construit la ref candidate d'un mirror (join `destination.path` + ref + suffixe) : c'est plus compliqué que ça (multi-cluster, digest) et c'est décrit dans le walkthrough, donc juste pointer dessus. (thread PR du 06/08 + note de meeting du 12/08)
4. [ ] renommer `unusedImageExpiry` => `unusedImageRetention` (décidé en meeting) dans spec.md, status.md et examples/06.
5. [ ] examples/03 : enlever les 2 excludes `charts-syncer` et `minideb` du côté v2 (décidé en meeting), garder `sealed-secrets`.
6. [ ] appliquer les 3 suggestions de review du 06/08 restées ouvertes : (a) enum `provider` — enlever la justification sur l'absence de `exec:` credential helper, garder l'info utile (liste fermée de registries cloud, qui ont leur propre méthode d'auth) ; (b) `injectPullSecret` — généraliser "12h ECR tokens" en "cloud-provider registries short lived tokens" ; (c) candidate ordering — simplifier le bullet du tri par nom en "sorting CR by name to handle possible overlapping config with deterministic alternatives order".

## Décisions à trancher

7. [ ] contradiction sortie de la passe de dédup multi-cluster : le walkthrough A.5 dit "Everything is copied, for every architecture: a verbatim copy of the full index and all its children" alors que `platforms.mode` vaut `Auto` par défaut (index filtré, donc digest différent, ce que la section multi-cluster de spec.md décrit noir sur blanc). soit A.5 devient "verbatim seulement en `platforms.mode: All`", soit `platforms.mode` disparaît. troisième position à arbitrer en même temps, sortie de la tâche 2 : rev4 §3.6 dit qu'un ImageMirror **ne se propose pas comme candidat** pour une ref pinnée si `platforms.mode != All` (donc jamais avec le défaut `Auto`), là où docs/v3 dit qu'une ref pinnée est copiée en index complet quel que soit `platforms.mode` ("pinning wins"). les deux ne peuvent pas coexister, et ça décide si les images pinnées sont mirrorées par défaut ou pas.
8. [ ] proposition sur les annotations (thread spec.md:618, sans réponse) : ne pas peupler `rewritten-by` ni `reason` pour un container qu'on n'a pas réécrit, et ajouter `kuik.enix.io/no-alternatives: ["<container>"]` pour garder la trace.
9. [ ] nommage de `rewritePolicy` (thread spec.md:39, sans réponse) : `evaluationOrder: PreserveOrder | PreferOriginal`, `PrimaryFirst | OriginalFirst`, ou on garde `OnFailure | Always`. sinon le déplacer dans open-questions.md.
10. [ ] persistance du budget de copie : rev4 §3.4 la met dans une ConfigMap `kuik-registry-budget` (une clé `data` par registry, `lastCopy`, persist-before-copy, "a restart that forgot it could exceed the budget"), docs/v3 la garde en mémoire avec des fenêtres comptées depuis le lancement du process. les deux empêchent le burst au redémarrage (la première fenêtre est à un `interval` complet du démarrage), donc l'in-memory a l'air suffisant et évite un objet non-CRD muté à chaque copie + le RBAC configmaps qui va avec. si on garde l'in-memory, le dire dans spec.md pour que la ConfigMap de rev4 ne revienne pas par la fenêtre.

## Réponses à donner sur la PR #629

11. [ ] thread sur `cycleStarted` (status.md:64, "un ring n'a ni début ni fin, est-ce que ça veut dire quelque chose ?") : avec `cycleDuration` mesuré, la borne du tour est le retour du curseur à sa position de départ, donc `cycleStarted` est le début du tour courant et porte bien une information.
12. [ ] commentaire sur `cycleDuration` : la question est tranchée (fenêtres par host, ring + curseur par (resource, host), tour mesuré), donc le poster en "voilà ce qui a été décidé et pourquoi" plutôt qu'en question ouverte. brouillon de la version "question ouverte" dans le scratchpad de la session.
13. [ ] signaler l'écart assumé sur le scheduling : le meeting parlait d'un limiteur glissant (`last_check = now` réarmé à chaque image), la doc décrit une fenêtre à phase fixe (tâche 18). à faire valider par Aurélien, puisque le glissant fausserait le `cycleDuration` mesuré.

## rev4 vs docs/v3

14. [ ] comparer l'identité canonique de rev4 (§1.6 : classes d'équivalence, ref canonique = premier préfixe du premier ImageAlternative en ordre lexico, "mirror refs are never representatives", canonicalisation en 3 étapes annotation -> strip destination + `_<clusterID>` -> normalisation) avec ce que dit docs/v3, qui utilise l'origin ref de l'annotation. ça touche le desired state du mirror, l'attribution et les clés de status. soit on l'intègre, soit on en fait une entrée dans open-questions.md.
15. [ ] décider si docs/v3/kuik-v3-adunand-rev4.md rentre dans la branche (actuellement untracked). si oui, dire dans le fichier ou dans un README ce qu'il est par rapport à spec.md (document de design complet dont spec.md est la condensation), pour éviter que les deux soient lus comme concurrents.

## Fait

16. [x] documenter le fait qu'on rajoute les unused images découvertes via un list des tags dans le status.pendingDeletion des mirror (ramasse-miettes), comme ça, si des images arrêtent d'être utilisées alors que le controlleur est down, c'est réconcilié au démarrage. en gros à chaque reconciliation d'un imagemirror, on commence par lister les tags, on regarde ceux qui ne sont plus utilisés, on les mets en pendingDeletion.
17. [x] update la doc pour dire que les interval par registry sont calculés depuis le lancement du pod, pas par rapport à l'Epoch unix 0.
18. [x] scheduling : fenêtre fixe retenue plutôt que le limiteur glissant du meeting (`last_check = now` réarmé à chaque image) — la phase est ancrée au démarrage du process (`start + k * interval`), le travail fait dans une fenêtre ne décale pas les suivantes, et une fenêtre ouverte sans rien à faire est perdue plutôt que capitalisée. couvre le ring du monitoring et la queue de copie.
19. [x] enlever la possibilité de choisir la method dans le check (HEAD|GET) : si une registry est pas spec compliant, c'est pas notre problème. on fait un HEAD dans tous les cas.
20. [x] documenter et avertir sur la gestion du quota sur du multi-cluster (cf. les interval) : en utilisant le même secret sur plusieurs clusters, on a le risque de taper la rate-limit si c'est mal configuré
21. [x] renommer excludeImagePrefix => excludeImages (on fait du globbing plutot que du prefix)
22. [x] mirror loop prevention : un mirror n'exclut que **son propre** `destination.path`, pas l'union des path de tous les mirror. raison : on ne mirror que l'image d'origine (annotation, ou la spec du pod si pas d'annotation), donc pas de loop possible entre deux mirror. seul cas restant : un user qui met explicitement une ref de mirror comme image d'origine — non supporté, et ne boucle pas non plus (A copie l'origine, B n'utilise que l'origine, qu'il exclut lui-même). (thread spec.md:218)
23. [x] multi-cluster : passe de dédup avec le walkthrough ImageMirror (l'info est répétée entre les deux), en gardant explicite le *pourquoi* du tag par cluster — même repository donc si le digest est déjà présent on ne crée qu'un tag, zéro layer poussé (bande passante), et le manifest ImageMirror reste identique sur tous les clusters. (thread spec.md:250)
24. [x] ring + curseur par (resource, host) et non par host, dédup des refs communes par cache de verdicts en mémoire, `cycleDuration` = tour mesuré depuis `cycleStarted` (solution A, sans réintroduire `maxPerInterval`).
25. [x] la destination d'un mirror n'est cadencée par rien : on ne limite que ce qu'on lit chez les autres. suppression du ring de self-check, `status.selfCheck` remplacé par un `selfChecked`.
